# 专题与规模化目录发布审计

## 范围

- 分支：`feat/topic-hubs-scalable-catalog-release`
- 起始提交：`d81c777`
- 最终提交：本文件所在提交，以 Git 历史为准
- 目录：319 张专辑、274 位艺人、13 张带 RYM 评分、11 张带相关流派

## 实施结果

详情页语义 DOM 已把“同艺人其他专辑”置于“继续探索”之前；空区块不渲染。新增 15 个核心流派、24 个真实相关流派、7 个本站场景和 7 个实际年代详情页，以及 3 个索引页。发现与专题每页 48 张，搜索每页 40 张，URL 保存页码且筛选、排序或查询变化时复位。

列表继续只消费不含曲目的轻量摘要；单分片 manifest 记录版本、数量、体积和哈希。合成 1k/10k/50k 基准全部完成，原始结果只在被忽略的缓存中。网易云同步增加平台验证分类、带哈希缓存、offline 与 verify-cache 模式；未使用 Cookie、Token、Authorization 或绕过。

`release:prepare` 只在干净提交上运行，使用临时目录完成质量门、构建、静态 HTTP、双包与 commit 验证后才替换有效 ZIP。内部 manifest 与外部 ZIP 哈希分离，避免自引用。

## 首轮验证

- lint、typecheck、46 个测试文件与 843 个用例：通过；
- production build：通过，663 个生成任务、660 个可部署 `index.html`；
- `git diff --check`：通过（仅有 Git 的 LF/CRLF 提示，无空白错误）；
- offline dry-run：10/10 保持不变，网络请求 0；
- verify-cache：空缓存 0/0 有效，目录修改为 false；
- 匿名在线冒烟：5 次专辑网络请求（3 次平台验证、2 次非预期形态），3 次艺人缓存命中；状态 `PARTIAL`，稳定目录未变化；
- Blocker：0；
- Major：0。

最终静态浏览器验收、发布包尺寸与 SHA-256 在最终提交后的 `release:prepare` 结果及交付报告中记录。已知限制是逐专辑纯静态导出的线性体积与时间，数千至数万目录需重新评估部署架构。
