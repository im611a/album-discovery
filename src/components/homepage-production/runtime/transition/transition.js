export function updateTransition(root,stageTop){const start=window.innerHeight,end=window.innerHeight*.65;const progress=Math.max(0,Math.min(1,(start-stageTop)/(start-end)));const copyright=root.querySelector(".ad-copyright");const hint=root.querySelector(".ad-scroll-hint");if(copyright)copyright.style.opacity=String(1-progress);if(hint)hint.style.opacity=String(1-progress);return progress}
